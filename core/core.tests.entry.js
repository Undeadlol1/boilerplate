import 'babel-polyfill'
import chai from 'chai'
import slugify from 'slug'
chai.should();


export default describe('EXAMPLE OF CORE TESTS', function() {

    // it('fails the test', function(done) {
    //     throw new Error('this is fine.jpg')
    // })

})