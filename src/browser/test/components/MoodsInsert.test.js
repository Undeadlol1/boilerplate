import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import MoodsInsert from '../../components/MoodsInsert';

describe('<MoodsInsert />', () => {
  // it('has <CircularProgress />', () => {
  //   const wrapper = shallow(<MoodsInsert />);
  //   expect(wrapper.find(CircularProgress)).to.have.length(1);
  // });

  // it('has `.MoodsInsert` class', () => {
  //   const wrapper = shallow(<MoodsInsert />);
  //   expect(wrapper.find('.MoodsInsert')).to.have.length(1);
  // });
});